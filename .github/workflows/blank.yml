
name: Code Quality Check
on: [push] # Runs on every single push to any branch

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Linter
        run: |
          echo "Running your linter here (e.g., npm run lint or flake8)"
          # If this command exits with an error, the push 'fails' the check
